# Override the API/WebSocket base URLs (default: production endpoints in src/services/api/config.js).
# Copy this file to .env and set values to point at a different backend, e.g. a local dev server.
# VITE_API_BASE_URL=http://localhost:8080/api/v1
# VITE_WS_BASE_URL=ws://localhost:8080/api/v1
